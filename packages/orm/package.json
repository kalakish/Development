{
  "name": "@nova/orm",
  "version": "1.0.0",
  "description": "ORM Layer for NOVA Framework",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nova/core": "^1.0.0",
    "@nova/metadata": "^1.0.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.5",
    "typescript": "^5.4.5"
  }
}
