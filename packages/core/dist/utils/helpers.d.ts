export declare class Helpers {
    static generateId(prefix?: string): string;
    static generateShortId(length?: number): string;
    static generateNumericId(length?: number): string;
    static generateTimestampId(): string;
    static truncate(str: string, length: number, suffix?: string): string;
    static camelCase(str: string): string;
    static pascalCase(str: string): string;
    static kebabCase(str: string): string;
    static snakeCase(str: string): string;
    static pluralize(str: string): string;
    static formatNumber(num: number, decimals?: number): string;
    static formatCurrency(num: number, currency?: string): string;
    static formatPercent(num: number, decimals?: number): string;
    static randomInt(min: number, max: number): number;
    static randomDecimal(min: number, max: number, decimals?: number): number;
    static formatDate(date: Date, format?: string): string;
    static daysBetween(date1: Date, date2: Date): number;
    static addDays(date: Date, days: number): Date;
    static addMonths(date: Date, months: number): Date;
    static addYears(date: Date, years: number): Date;
    static startOfDay(date: Date): Date;
    static endOfDay(date: Date): Date;
    static startOfMonth(date: Date): Date;
    static endOfMonth(date: Date): Date;
    static deepClone<T>(obj: T): T;
    static deepMerge<T>(target: T, source: Partial<T>): T;
    static omit<T extends object, K extends keyof T>(obj: T, keys: K[]): Omit<T, K>;
    static pick<T extends object, K extends keyof T>(obj: T, keys: K[]): Pick<T, K>;
    static isEmpty(obj: any): boolean;
    static chunkArray<T>(array: T[], size: number): T[][];
    static uniqueArray<T>(array: T[]): T[];
    static groupBy<T>(array: T[], key: keyof T): Record<string, T[]>;
    static sortBy<T>(array: T[], key: keyof T, direction?: 'asc' | 'desc'): T[];
    static isValidEmail(email: string): boolean;
    static isValidUrl(url: string): boolean;
    static isValidPhone(phone: string): boolean;
    static formatBytes(bytes: number, decimals?: number): string;
    static formatDuration(ms: number): string;
}
//# sourceMappingURL=helpers.d.ts.map